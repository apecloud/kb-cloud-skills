# engineNetworkModeSupported

**Type:** object

## Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `supported` | networkMode[] | No | supported network modes |
| `notSupportedVersions` | object | No | not supported versions, key is networkMode, value is array of versions |

