# clusterRawLogResponse

Cluster raw log is the raw log of the cluster

**Type:** object

## Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `filenames` | string[] | No |  |
| `items` | clusterRawLogItem[] | No |  |
| `pagination` | [clusterLogPagination](clusterLogPagination.md) | No |  |

