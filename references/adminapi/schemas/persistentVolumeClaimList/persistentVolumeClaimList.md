# persistentVolumeClaimList

the List stands for stats for persistentvolumeclaims.

**Type:** object

## Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `items` | persistentVolumeClaim[] | No |  |
| `currentPage` | integer (int64) | No | the current page number |
| `totalPage` | integer (int64) | No | the total page number |
| `pageSize` | integer (int64) | No | the page size |

